{
  "name": "postgres",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": [],
  "targets": {
    "podman-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "podman build -t postgres -f Containerfile .",
        "cwd": "{projectRoot}"
      }
    }
  }
}
